apiVersion: 1
datasources:
  - name: postgres
    type: postgres
    access: proxy
    url: $TEST_DB_HOST:$TEST_DB_PORT
    user: $TEST_DB_USER
    database: $TEST_DB_NAME
    isDefault: true
    editable: true
    uid: "bdgisvc9bvym8brelease"
    jsonData:
      postgresVersion: 1400
      timescaledb: false
      maxOpenConns: 5
      sslmode: "disable"
    secureJsonData:
      password: $TEST_DB_PASSWORD
